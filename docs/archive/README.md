# Archive

Documents that are superseded but historically useful. Nothing here should be treated as current.

| File | Why archived | Replaced by |
|---|---|---|
| [dashboard-mock.html](dashboard-mock.html) | Design-era static mock of the SOC console; the real console now lives in `ui/` and is served at `/dashboard`. | [../components/Console_UI.md](../components/Console_UI.md), the `ui/` app |
