interface:
  display_name: "AegisAgent"
  short_description: "Repo-specific patterns and workflows for AegisAgent"
  default_prompt: "Use the AegisAgent repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true