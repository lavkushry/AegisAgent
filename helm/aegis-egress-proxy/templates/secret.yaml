{{- if .Values.secret.create }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "aegis-egress-proxy.fullname" . }}
  labels:
    {{- include "aegis-egress-proxy.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- if .Values.secret.apiToken }}
  AEGIS_API_TOKEN: {{ .Values.secret.apiToken | quote }}
  {{- end }}
{{- end }}
