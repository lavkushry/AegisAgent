apiVersion: v1
kind: Service
metadata:
  name: {{ include "aegis-llm-gateway.fullname" . }}
  labels:
    {{- include "aegis-llm-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "aegis-llm-gateway.selectorLabels" . | nindent 4 }}
