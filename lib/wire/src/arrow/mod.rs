//! Arrow logical schemas for the v2 analytical output (ADR-0011).
//!
//! `event_schema` is the security-event schema (LLD §9.2). `query_schema`
//! (LLD reference layout) is a later increment.

pub mod event_schema;
